# expo-share-intent's latest release declares support for Expo SDK 56 while
# this project is on SDK 57 (it works fine — only native config is consumed,
# and via prebuild). Without this flag a plain `npm install` fails with an
# ERESOLVE peer-dependency conflict and leaves node_modules incomplete.
legacy-peer-deps=true
